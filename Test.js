function storeNames() {
    return arguments;
}

// When we run the following line in the console:
storeNames("Mina", "Bab", "AnidCo");

// The output will be { '0': 'Bab', '1': 'AnidCo', '2': 'Mina' }
